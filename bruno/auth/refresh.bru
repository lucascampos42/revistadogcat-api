meta {
  name: Refresh Token
  type: http
  seq: 5
}

post {
  url: {{host}}/auth/refresh
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

vars:pre-request {
  refreshToken: 
}
